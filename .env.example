# .env.example — Copy to .env locally. NEVER commit .env
# Generate values with: openssl rand -hex 32

HORUS_SECRET_TOKEN=
HORUS_TLS_PASSWORD=
SERVER_PRIVATE_KEY=
REDIS_PASSWORD=
TURN_USERNAME=
TURN_PASSWORD=
