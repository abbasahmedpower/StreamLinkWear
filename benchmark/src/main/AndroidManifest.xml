<?xml version="1.xml" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.streamlink.benchmark">

    <!-- Benchmark requires queries to launch the app -->
    <queries>
        <package android:name="com.streamlink.app" />
    </queries>

</manifest>
